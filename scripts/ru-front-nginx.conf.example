# Shop/API are not hosted on a VPN-stream node.
# Domain traffic: panel or Always-Free HTTP front
# (scripts/panel-front-nginx.conf.example + setup-panel-front.sh).
# Stream public 443 is nginx SNI only — see ru-front-stream.conf.example.
